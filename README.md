# Обход бинарного дерева (Binary Tree Traversal)

## Описание

Программа для работы с бинарными деревьями с интерактивным интерфейсом. Позволяет создавать несколько именованных деревьев, добавлять узлы вручную, выполнять все основные виды обхода (включая обход в ширину) и проверять дерево на симметричность.

## Структура проекта

### `TreeNode` - узел дерева

```python
class TreeNode:
    value    # значение узла (целое число)
    left     # ссылка на левое поддерево
    right    # ссылка на правое поддерево
```

### `MapTree` - хранилище деревьев

```python
class MapTree:
    map      # словарь, где ключ - название дерева, значение - корень (TreeNode)
```

Позволяет хранить несколько независимых деревьев и переключаться между ними.

## Функции для работы с хранилищем

| Функция | Описание |
|---------|----------|
| `addtree(name, koren)` | Добавляет дерево с указанным названием в хранилище |
| `getTree(name)` | Возвращает корень дерева по названию или None |
| `printNames()` | Выводит названия всех сохраненных деревьев |

## Функции обхода дерева

| Функция | Описание | Порядок обхода | Сложность |
|---------|----------|----------------|-----------|
| `preorder(root)` | Прямой обход (NLR) | Корень -> Левое -> Правое | O(n) |
| `inorder(root)` | Симметричный обход (LNR) | Левое -> Корень -> Правое | O(n) |
| `postorder(root)` | Обратный обход (LRN) | Левое -> Правое -> Корень | O(n) |
| `bfs(root)` | Обход в ширину (BFS) | По уровням сверху вниз | O(n) |
| `check_symmetry(node)` | Проверка симметричности | Сравнивает симметричный обход с его реверсом | O(n) |

## Интерактивное меню

Программа поддерживает интерактивное меню с выбором действий:

```
0. Выбрать текущее дерево
1. Создать дерево
2. Добавить узел в существующее дерево
3. Проверить симметрию дерева
4. Вывести все названия деревьев
5. Провести прямой обход дерева и вывести его
6. Провести симметричный обход дерева и вывести его
7. Провести обратный обход дерева и вывести его
8. Сделать обход в ширину и вывести его
Все остальные значения - закончить работу с программой
```

## Быстрый старт

```python
# Создаем дерево вручную
root = TreeNode(1)
root.left = TreeNode(2)
root.right = TreeNode(3)
root.left.left = TreeNode(4)
root.left.right = TreeNode(5)

# Прямой обход
print(preorder(root))   # [1, 2, 4, 5, 3]

# Симметричный обход
print(inorder(root))    # [4, 2, 5, 1, 3]

# Обратный обход
print(postorder(root))  # [4, 5, 2, 3, 1]

# Обход в ширину
print(bfs(root))        # [1, 2, 3, 4, 5]

# Проверка на симметрию
print(check_symmetry(root))  # False
```

## Пример работы с программой

```
Выберите, что хотите сделать с деревьями и их узлами
0. Выбрать текущее дерево (Сейчас выбрано дерево: None)
1. Создать дерево
...

> 1
Введите название нового дерева
> mytree
Введите значение его корня
> 10
Добавлено

> 0
Введите название дерева, с которым хотите работать
> mytree
Выбрано mytree

> 2
Сейчас вы находитесь в узле со значением 10
Введите в какое поддерево хотите добавить узел (l или r)
> l
Мы дошли до свободного узла. Введите его значение
> 5
Добавлено

> 2
Сейчас вы находитесь в узле со значением 10
Введите в какое поддерево хотите добавить узел (l или r)
> r
Мы дошли до свободного узла. Введите его значение
> 15
Добавлено

> 5
[10, 5, 15]

> 8
[10, 5, 15]

> 3
True
```
